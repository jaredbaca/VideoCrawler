package springboot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * This class is the main entry point for Spring Boot, which allows the Java code
 * to be hosted on a localhost:8080. When running the project as a Spring Boot Project,
 * this is the class that must be selected to run.
 * @author jbaca
 *
 */

@SpringBootApplication
public class BacaCs622FinalProjectApplication {

	public static void main(String[] args) {
		SpringApplication.run(BacaCs622FinalProjectApplication.class, args);
	}

}
